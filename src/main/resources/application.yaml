spring:
  application:
    name: encryption
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/encryption}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: ${PORT:8080}

app:
  encryption:
    enabled: true
    client-origin: http://localhost:3000
    timestamp-skew-seconds: 300
    nonce-ttl-seconds: 300
    server-private-key-pem: ${APP_SERVER_PRIVATE_KEY_PEM:}
    server-public-key-pem: ${APP_SERVER_PUBLIC_KEY_PEM:}
